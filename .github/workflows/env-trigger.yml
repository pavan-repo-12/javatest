name: Deploy Multi-Environment

on:
  push:
    branches:
      - feature/javahelm

jobs:
  deploy-dev:
    uses: pavan-repo-12/javatest/.github/workflows/maven-publish.yml@feature/javahelm  # Job-level call
    with:
      envvalue: dev


  # deploy-test:
  #   if: ${{ github.event_name == 'workflow_dispatch' }}
  #   needs: deploy-dev
  #   uses: ./.github/workflows/build-and-deploy.yml
  #   environment: qa

  # deploy-prod:
  #   if: ${{ github.event_name == 'workflow_dispatch' }}
  #   needs: deploy-test
  #   uses: ./.github/workflows/build-and-deploy.yml
  #   environment: prod