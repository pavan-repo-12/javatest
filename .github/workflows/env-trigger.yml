name: Deploy Multi-Environment

on:
  push:
    branches:
      - feature/javahelm

jobs:
  deploy-dev:
    uses: ./.github/workflows/maven-publish.yml  # Job-level call
    runs-on: self-hosted                           # Must match runner type
    environment: dev

  # deploy-test:
  #   if: ${{ github.event_name == 'workflow_dispatch' }}
  #   needs: deploy-dev
  #   uses: ./.github/workflows/build-and-deploy.yml
  #   environment: test

  # deploy-prod:
  #   if: ${{ github.event_name == 'workflow_dispatch' }}
  #   needs: deploy-test
  #   uses: ./.github/workflows/build-and-deploy.yml
  #   environment: prod